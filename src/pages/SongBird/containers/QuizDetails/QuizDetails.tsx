import React from "react"

import { QuizDetails as BaseQuizDetails } from "../../components"

interface Props {}

const QuizDetails = (props: Props) => {
  return <BaseQuizDetails />
}

export default QuizDetails
